jwt:
  secret: "JWT_SECRET_LUCKY_MARKET"
  ttl: '7d'

postgres:
  type: "postgres"
  host: "localhost"
  port: 5432
  username: "postgres"
  password: "postgres"
  database: "lucky_market"

mongo:
  uri: "mongodb://localhost:27017"
  dbName: "lucky_market"

redis:
  host: "localhost"
  port: 6379
  dbs:
    default: 0
    auth: 1

# use bcrypt for hashing
hashing:
  rounds: 8

# for signing cookies
cookie-secret: "COOKIE_SECRET_LUCKY_MARKET"

# in milliseconds
ttls:
  session: 604800000